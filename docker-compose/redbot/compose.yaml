services:
  redbot:
    container_name: redbot
    image: phasecorex/red-discordbot
    restart: ${RESTART_POLICY}
    volumes:
      - ${CONFIG_PATH}/redbot:/data
    environment:
      - TOKEN=${DISCORD_TOKEN}
      - PREFIX=.
      - TZ=Europe/Bucharest
      - PUID=1000
    deploy:
      resources:
        limits:
          memory: 200M
        reservations:
          memory: 64M
